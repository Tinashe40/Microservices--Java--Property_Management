package com.tinash.cloud.utility.enums;

/**
 * Enumeration for different types of user roles within the application.
 */
public enum RoleType {
    ADMIN,
    USER,
    PROPERTY_MANAGER,
    TENANT,
    GUEST
}
